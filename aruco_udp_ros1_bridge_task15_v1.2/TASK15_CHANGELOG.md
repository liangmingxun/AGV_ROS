# Task 15修改记录

- UDP保持v3和CRC/session/seq/三时刻语义。
- 实体冻结为AGV1/2/3=`1/2/3`、载荷=`0`；载荷不是Robot4。
- ArUco字典改为`DICT_4X4_50`。
- 删除旧的像素到世界单应性标定和点击标定工具。
- 新增四固定地面码`8/11/15/30`在线外参：
  ID 8为原点，8→11为+X，ID 30选择+Y，ID 15参与平面拟合/监测。
- 地面码150 mm提供米制尺度；无需预先填写固定码世界坐标。
- 车载码80 mm，AGV码平面高度225 mm。
- 采用已有1280×1024内参与畸变参数，通过点模型矫正；禁止静默改变
  图像几何。
- Windows只发送`world_T_tag`，标记到车体/载荷的完整SE(2)仍完全属于
  Task 15适配器。
- Task 15保留raw/filtered、confidence门、时间戳/超时和
  CooperativeState职责。
- 载荷接口保留，但在载荷高度和刚体变换测量前禁用正式发送。
- Robot1新增地面标定模式、ID顺序、角色、边长和实体/地面ID不重叠校验。
- 修正标记航向为ArUco对象坐标`+X`，避免90°固定偏差。
- 未引入TF、CooperativeState、底盘命令或自动执行授权。
