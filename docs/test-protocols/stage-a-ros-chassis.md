# Stage A：ROS 与车端执行链门禁

状态：**未执行，未签字，不构成通过。**

前提：车轮架空或与载荷机械断开；三车急停可触达；正式算法硬件门保持 `false`。

- [ ] 保存软件回归结果及 `roslaunch --files` 解析结果。
- [ ] 三车命名空间隔离，每个 `/agvN/chassis_command` 恰有一个发布者，且无 `move_base`。
- [ ] 验证 250 Hz 执行循环、100 Hz 反馈/能力报告及 SI↔串口单位换算。
- [ ] 分别验证速度限幅、非对称加减速、先回零再反向和最终安全限幅。
- [ ] 人工急停使三车输出归零；断开状态/能力输入时中央链 fail-zero。
- [ ] bag 含 raw/applied/actual、能力、命令、状态和实验窗口；校验报告通过。

证据目录：`________`　日期：`________`　操作者：`________`　复核：`________`

任一项失败：停止，归零，不进入 Stage B。
