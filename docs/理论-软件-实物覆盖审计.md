# 理论—软件—实物覆盖审计

审计基线：`实物实验设计_v1.2.1_接口冻结版.md` 与当前平台基础分支。本文只记录仓库中可由代码或自动测试证明的状态；既往实车标定结果仍须将数值和原始记录归档后才算正式冻结。

## 当前结论

当前代码完成的是实施计划 Task 0–7，即三车 ROS 平台基础层和单车执行链。它与实验设计中的接口冻结、命名空间、车端限幅、平滑降额、raw/applied/actual 反馈、轮速与 IMU 预里程计相对应，可以进入“单车举升低速验证”。

当前代码不是完整协同搬运控制系统，尚不能开展正式实验 1、2a、2b、3，也不能声称理论算法已经全部实物实现。路径、统一状态、能力映射、上下层控制、实验监督、正式定位与数据处理仍属于 Task 8–18。

## 逐项覆盖矩阵

| 实验设计/理论项 | 当前软件证据 | 状态 | 实物或数据门槛 |
|---|---|---|---|
| §1.4、§21.0 三车命名空间与冻结接口 | `agv_msgs` 八个消息；三车 launch；接口字段和队列自动测试 | 已实现 | 三主机发现、时钟同步和非锁存频率仍须实车记录 |
| §2.2 车端单一职责 | `chassis_controller` 只接收本车命令/降额，不含车间算法或 UDP 接收 | 已实现 | 禁止同时启动旧 `keyboard_ctrl`、`nav/move_base` 等命令源 |
| §2.3 250 Hz 执行、100 Hz 状态 | 250 Hz 单调时钟循环和独立 100 Hz 发布调度 | 已实现并测试 | 实车记录 overrun、消息频率与 CPU 负载 |
| §3.2 轮速+IMU 预里程计 | 轮速平移、校正后陀螺角速度、里程计和 TF；静止复位服务 | 部分实现 | 仅用于预实验；无世界坐标统一状态，复位的“实验未运行”门控待 supervisor 实现 |
| §3.1、§3.3、§3.4 统一状态与正式定位 | 尚无 `pose_provider`、`path_state_estimator` 和顶部相机适配 | 未实现 | Task 9、15 后才可用于正式轨迹指标 |
| §4 S 形路径、弧长与支撑点参考 | 支撑点 TF 偏置可按车在 launch 中配置 | 部分实现 | S 曲线、投影、非退化校核和支撑几何文件待 Task 8；偏置需实测归档 |
| §5.1–§5.4 平滑降额与车端能力 | `DeratingCommand`、幂等序列处理、平滑下降/恢复、`CapabilityReport` | 已实现并测试 | car2 举升验证实际轮速、applied 和报告比例；正式触发器未实现 |
| §5.5–§5.8 车端能力到路径能力 | 尚无 `capability_mapper`、报告年龄和延迟预算统计 | 未实现 | Task 10、12、17 |
| §6.3–§6.7 物理执行接口 | SI 制轮线速度命令、左右非对称速度/加/减速度限幅、反向先到零、原串口协议、三层反馈 | 已实现并测试 | STM32 协议未改变；必须举升核对方向、单位换算和串口设备 |
| §6.1–§6.2 完整二维下层 | 尚无平面支撑点跟踪器和论文下层控制器 | 未实现 | Task 11、14 后才可声称完整下层 |
| §7 机械托盘和支撑 | 只有每车 `support_link` TF | 部分实现 | 刚性托盘、支撑点坐标、装配重复性需物理测量 |
| §8.1–§8.2 几何及动力标定 | YAML 保留轮径、轮距和左右能力参数 | 待证据冻结 | 用户已做过验证，但当前三车数值相同；需逐车核对并保存日期、方法、数据与最终值，不能仅凭代码默认值认定完成 |
| §9 安全 | 启动超时、串口写失败退出、速度硬上限、错误配置拒绝、人工停机流程 | 部分实现 | 当前按设计没有通信失联自动停车、急停闭环或 supervisor 状态机，不得宣称已实现 |
| §10 阶段 A | fake 三车、隔离、TF/命令唯一权和车端执行链已通过自动测试 | 软件门槛通过 | 仍须按检查手册完成单车举升硬件门槛 |
| §10 阶段 B–D、§11–§17 正式算法与实验 | 对应算法包和实验模式不存在 | 未实现 | 严格按 Task 8–18 顺序推进，不可跳到载荷或正式采集 |
| §18–§20 数据、指标、图表 | 现有消息包含部分原始字段，但无 `data_logger` 和离线指标流水线 | 未实现 | Task 17，且需保存元数据、参数快照和 Git SHA |

## 当前代码已修正的问题

- 冻结消息测试由两个消息扩展为全部八个消息的字段精确校验。
- 话题队列与 §21.0.1 对齐：命令 1，反馈/能力/降额 5，里程计/IMU 10，均不锁存。
- 反馈和能力序列号改为每次发布递增；重复的同序列降额命令改为幂等，不再反复重启斜坡。
- 增加 `/agvX/reset_odometry`，仅在 raw、applied、actual 轮速均接近零时允许复位。
- 串口初始化增加超时，写失败不再以成功码静默退出；串口速度安全上限与上报能力保持一致。
- QEKF 使用校正并滤波后的陀螺数据；非法 IMU 标定、车号和冻结坐标系在启动时直接报错。
- 支撑点偏置由硬编码改为各车 launch 参数，便于后续实测冻结。

## 下一阶段唯一推荐顺序

1. 按 `三车Ubuntu移植与上车检查手册_v1.0.md` 完成单车 fake、单车举升和串口检查，并归档三车标定表与 rosbag。
2. 只有举升门槛全部通过后，做单车空载、低速、短距离落地测试；当前不能直接上托盘。
3. 实现 Task 8：S 形弧长路径和支撑几何，并用纯数学测试冻结。
4. 依次实现 Task 9–12，打通统一状态、能力映射、单车跟踪和位置触发降额的因果链。
5. 再实现 Task 13–17 的对比方法、完整下层、相机、M2b 和数据流水线；最后按 Task 18 分阶段做三车与载荷实验。

## 不应误用的旧代码

仓库中的旧导航、建图和键盘控制包为历史功能，接口仍可能使用 `/cmd_vel` 或 `/agv_chassis/cmd_vel`。它们没有纳入冻结执行链，正式测试时不得与 `multi_agv_bringup` 同时作为控制源。当前底层串口帧格式保持原模式，但 ROS 上层入口已经冻结为每车 `/agvX/chassis_command`，因此不能用旧键盘节点来证明新链路合格。
