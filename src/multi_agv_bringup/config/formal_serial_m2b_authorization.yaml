# M2b+M2b method-specific physical gate. On 2026-09-08 the operator authorized
# direct comparison on the already calibrated R=0.7 m full circle, unloaded
# and without Robot2 derating, after the shared execution/safety chain and
# M2b fake E2E were reviewed. Formal statistical approval remains separate.
formal_upper:
  hardware_execution_authorized: true

formal_lower:
  hardware_execution_authorized: true

authorization_scope:
  path_config: src/multi_agv_bringup/config/path_circle_r0p7_cw_m1_r1.yaml
  runtime_config: src/multi_agv_bringup/config/formal_serial_m1_r1_circle_r0p7_runtime.yaml
  evaluation_config: src/multi_agv_bringup/config/formal_evaluation_circle_r0p7.yaml
  target_progress: 4.998229715025710
  robot2_derating: false
